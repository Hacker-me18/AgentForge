# AgentOS Studio MVP 核心依赖（Python 3.11）
# 后续 Phase 按需追加（如 docker SDK 等）

# --- API / Web ---
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.8
pydantic-settings>=2.4

# --- 存储 ---
sqlalchemy[asyncio]>=2.0
aiosqlite>=0.20

# --- 网络 / 工具 ---
httpx>=0.27
pyyaml>=6.0
python-dotenv>=1.0

# --- 开发工具链 ---
pytest>=8.0
pytest-asyncio>=0.23
ruff>=0.6
mypy>=1.11
pre-commit>=3.8
